name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# A newer push to the same ref makes the running job irrelevant.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      # One platform failing should not hide the state of the others.
      fail-fast: false
      matrix:
        # MSVC is the one compiler that rejects C the others accept, and a
        # Helix user on Windows compiles src/scanner.c themselves.
        os: [ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: npm

      - run: npm ci

      # One platform only: git normalises line endings on the Windows
      # checkout and reports the whole file as changed.
      - name: Generated parser is up to date
        if: matrix.os == 'ubuntu-latest'
        run: ./scripts/check-generated.sh

      # Compiles src/scanner.c, the only hand-written C here.
      - name: Test
        run: npx tree-sitter test

      - name: Grammar parses its own Boo script
        run: npx tree-sitter parse scripts/bumpversion.boo

  corpus:
    name: parse rate
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: npm

      - run: npm ci

      # Pinned so a new compiler test cannot fail this job on unrelated work.
      # Bump deliberately, with the parse rate rechecked in the same commit.
      - name: Check out the compiler for its .boo corpus
        uses: actions/checkout@v7
        with:
          repository: boo-lang/boo
          ref: 26a0dfa2073b64cf43766dd6df2b3680ec7597dc
          path: boo

      - name: Parse rate
        run: ./scripts/parse-rate.sh boo
